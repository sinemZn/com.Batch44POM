package tests.homework;

public class C01 {
    //ESTNG POM
    //    Go to https://demoqa.com/ write an automated test
    // Alerts ( handle all alerts)
    // Interactions (Click droppable button
    // and under the simple module drag the “drag me” box to the   “drop me” box
    // and verify that dropped.)
    // Widgets ( click tool Tips button,
    // hover over the “Hover me to see” button
    // and verify the “You Hovered Over the button” message
    // then hover over the “Hover me to see” text field and verify “You hovered the text field” message.
}
